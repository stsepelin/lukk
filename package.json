{
  "name": "lukk-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation site (VitePress) for lukk — first-party Laravel JWT auth.",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vitepress-plugin-llms": "^1.0.0"
  }
}
